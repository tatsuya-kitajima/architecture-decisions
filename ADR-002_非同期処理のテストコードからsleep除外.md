---
created: 2026-02-28
updated: 2026-02-28
implemented: 2025-05-01
tags: ADR
---

# ADR-002: 非同期処理のテストコードからsleep除外

## 対応時期

2025/05頃

## 背景・課題

- 非同期処理を呼び出すテストコード内で、3秒のsleepが使用されていた
	- sleepを入れないと非同期処理が完了する前にassertしてしまい、モックが使用されずエラーとなるため
- それにより、以下の問題があった
	- テスト時間が長い
		- CIでの待機時間が長くなり、開発効率が悪い
	- 非同期処理が増える度にテストが遅くなり続ける構造

## 検討した選択肢

前提
- service層でgoroutineを実行する関数がある
- テストコードでその関数を呼び出した場合、goroutineより先にテストが完了するとモック未使用でエラーとなる

### WaitGroupを使う

- 採用：No
- 判断：
	- WaitGroupはあくまでgoroutineの完了を待機するだけで、呼び出し元(controller層やテストコード)にそれを伝播することはできない
	- そのため要件を満たせず却下

### エラーチャネルを返却し、呼び出し元でチャネルを消化する

- 採用：Yes
- メリット：
	- チャネルを返すことで、非同期処理の完了やエラーを管理・検証できるようになる
	- 非同期処理の完了まで待機することで、メモリリークの発生を抑制できる
	- sleepをなくすことができる
- デメリット：
	- goroutineを使用している全ての箇所に修正・動作確認が必要
- 判断：
	- 実装コストはかかるが、以下のメリットが大きい
	  - テストコード内のsleep待機（アンチパターン）を排除できる
	  - 非同期処理の完了を呼び出し元で制御できるため、メモリリーク抑止が可能
	  - 非同期処理が増えても テスト時間が線形に増加しない設計になる
	- これらは、システムの安全性と開発効率の両方に貢献する

---

## 結果

- より安全に非同期処理を実行できる仕組みを構築できた
- テストコード内のsleepをなくすことができた
	- テスト全件実行にかかる時間が短縮された
		- 5分くらい？→30秒くらい

## 学び

- goroutineのチャネルについての理解が深まった
	- チャネルの消化を適切に行わないとメモリリークが発生する
	- これまではチャネル管理していなかったため、問題を認識することすらできなかった