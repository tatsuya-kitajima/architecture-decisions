---
created: 2026-06-09
updated: 2026-06-09
implemented: 2026-06-01
tags: ADR
---

# ADR-008: フロントテスト性能改善

## 対応期間

2026年5月〜6月

## 背景・課題

[[ADR-001_フロントテストリファクタリング]]

上記対応にてテストファイルをコンポーネント単位に分割したことで、CI実行時間が約9分から約25分に増加した。

計測の結果、ファイル数の増加（+39%）に対してimport時間が約2倍以上（425s→887s）に膨らんでいることが判明した。Vitestのデフォルト設定（`isolate: true`）ではテストファイルごとにモジュールキャッシュをリセットするため、ファイル数が増えるほどimport・environment・setupのオーバーヘッドが累積する構造だった。

## 検討した選択肢

### 選択肢A: isolate: true のまま維持（デフォルト）

- 採用：No
- メリット：テスト間の副作用が完全に分離される。モック設計に制約がない。
- デメリット：ファイル数に比例してimport時間が増加する。今後もファイル分割が進むと悪化する一方。

### 選択肢B: isolate: false に変更

- 採用：Yes
- 採用理由：import時間の大幅削減が見込まれ、実測でも効果が確認できた。副作用のリスクはモック設計の改善で対処可能と判断した。
- メリット：テストファイル間でモジュールキャッシュを共有するため、import/environment/setupのオーバーヘッドが大幅に削減される。
- デメリット：テストファイル間でモジュール状態が共有されるため、前テストのモック状態や非同期処理の残留が次テストに影響する可能性がある。モック設計に一定の制約が生まれる。

### 選択肢C: pool: forks に変更

- 採用：No
- メリット：ファイルごとにプロセス分離されるため、isolate: falseの副作用を避けられる。
- デメリット：isolate: falseと組み合わせても実質的にプロセス分離が優先されるため、import時間の削減効果がない。デフォルトのisolate: trueと同等のパフォーマンスになる。

---

## 結果

### パフォーマンス改善

**CI総実行時間の推移**

- 改善前（isolate: true, シャード2, 1ランナー）: 約25分
- isolate: false 導入後（シャード2, 1ランナー）: 約13分
- ランナー構成変更後（シャード6, シャードごとに専用ランナー）: 約4分

**詳細計測値（シャード単位）**

|項目|改善前|改善後|
|---|---|---|
|import時間（Shard1）|910s|66s|
|environment時間（Shard1）|160s|4s|
|setup時間（Shard1）|41s|2s|

### 副作用への対処

`isolate: false` 導入後、テスト間のモック汚染が複数のパターンで発生した。以下の対処パターンを確立した。

**問題1: `mockedUseAuth0.mockReturnValue()` で新しいオブジェクトを渡すと前テストの非同期残留に消費される**

```typescript
// ❌ NG: 新しいオブジェクトを渡すと残留処理に消費される
mockedUseAuth0.mockReturnValue({ ...auth0Mock, logout: vi.fn() })

// ✅ OK: auth0Mockのプロパティを直接操作する
auth0Mock.logout.mockImplementation(...)
```

**問題2: Auth0認証エラーテストのモックが前テストの残留処理に消費される**

個別テストでの `mockReset()` ではなく、`testMocks.ts` の `beforeEach` でまとめてリセットすることで対処した。

**問題3: auth0Mockのプロパティ（isAuthenticated等）が前テストで書き換えられたまま残る**

`testMocks.ts` の `beforeEach` で各プロパティを直接上書きすることでリセットした。

問題2・3の対処をまとめた `beforeEach` の最終形は以下の通り。

```typescript
// testMocks.ts
beforeEach(() => {
  vi.clearAllMocks();
  // auth0Mockの各プロパティを直接初期値で上書き
  auth0Mock.isAuthenticated = true;
  auth0Mock.isLoading = false;
  auth0Mock.logout = vi.fn();
  // ... 全プロパティ
  mockedUseAuth0.mockReturnValue(auth0Mock);
  auth0Mock.getIdTokenClaims.mockResolvedValue({ __raw: 'idtoken' });
});
```

**問題5: setupFilesとテストファイルでモジュールインスタンスが異なる**

`isolate: false` 環境ではsetupFilesとテストファイルのモジュールインスタンスが異なる場合があり、同じ `import` 文でも別インスタンスを参照することがある。`globalThis` を介してモックオブジェクトを公開することで解消した。

```typescript
// testMocks.ts
const _mocks = vi.hoisted(() => {
  const navigate = vi.fn();
  (globalThis as Record<string, unknown>).__vitest_mockNavigate = navigate;
  return { navigate };
});
```

最終的にはすべてのファイルを `isolate: false` で通過させることができ、`projects` による個別分離は不要になった。

## 学び

**isolate: false の副作用はやってみないと分からない** テストファイル間で状態が共有されるため、モックの参照管理・プロパティのリセットを明示的に行う必要がある。ただし「導入前に設計を整備してから導入する」という順序は現実的ではなく、`isolate: false` で実際に動かしながら問題を発見・対処していく手順が結果的に最適だった。

**デバッグログが原因特定に最も寄与した** 実行ごとにエラーが出たり出なかったり、出てもエラーとなるファイルが変わったりという再現性の低い状況では、静的な分析では限界があった。テスト実行中にモックの状態や呼び出し回数をログ出力することで、「どの時点でどのモックがリセットされていないか」を特定できた。一見遠回りに見えても、デバッグログを仕込む判断が解決を大きく前進させた。

**原因調査には二分探索が有効** 「特定のファイル追加で再現する」問題は、`/tmp/default-files-only.txt` にファイル一覧を取得し、`sed -n '1,N p'` で範囲を絞り込む二分探索が効率的だった。ただし再現性が低いタイミング問題には効果が薄く、その場合はVerboseログから直前に実行されたファイルを特定するアプローチが有効。

**globalThisを使ったモック共有が安定化の鍵** `isolate: false` 環境ではsetupFilesとテストファイルのモジュールインスタンスが異なる場合があり、同じ `import` 文でも別インスタンスを参照することがある。`globalThis` を介してモックオブジェクトを公開することで、全テストファイルが確実に同一インスタンスを参照できる。

**モックのリセット戦略**

- `vi.clearAllMocks()`（beforeEachで手動）: setupFilesスコープのモックの呼び出し履歴をクリア
- プロパティの直接上書き（beforeEachで手動）: `isAuthenticated` 等のプリミティブプロパティをリセット
- この2つは役割が異なり補完関係にある。`resetMocks: true` はtestMocks.tsの `beforeEach` でのリセット処理と競合したため使用しなかった。

**passThrough() は jsdom 環境では使えない** `axios-mock-adapter` の `passThrough()` は実際のネットワークアクセスに依存するため、jsdom環境では動作しない。ネットワークエラーのテストには `vi.spyOn(axios, 'post').mockRejectedValueOnce(Object.assign(new Error(), { request: {} }))` を使う方が環境に依存せず安定する。
