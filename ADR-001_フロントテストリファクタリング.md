---
created: 2026-02-16
updated: 2026-02-16
implemented: 2026-01-01
tags: ADR
---

# ADR-001: フロントテストリファクタリング

## 対応時期

2025/11下旬～2026/01上旬

## 背景・課題
- 画面ごとにEventテスト(ロジック網羅)、Layoutテスト(スナップショット)が実装されていた
	- 機能が多い画面だと、1つのテストファイルが3万行を超えるほど膨大になっていた
- テストケースごとに全く同一の記述が多数あり、テストコードごとの差分を見分けることが困難だった
- 上記が組み合わさっており、以下のような問題があった
	- 差分が分かり辛いので、テストケースの重複や不要なテストケースに気付けない
	- テストの追加箇所の特定が困難
	- 修正が入る度に余計な工数がかかる

## 検討した選択肢

### ヘルパー関数化(採用)

コンポーネントごとにレンダリング用のヘルパー関数を作成し、差分のみ引数として与えて上書きする作りにする。

- メリット
	- ボイラープレートコードを無くせる
	- テストごとの差分が分かりやすくなる
- デメリット
	- 特になし

他にも、細かい設計判断があった
- 各テストファイルで行っていたモックセットアップをまとめてファイル化し、全テストのセットアップ時に実行するようにした
	- コンポーネントごとに分割することでファイルごとにそれらのボイラープレートコードが記述され、冗長化するのを防ぐため

### コンポーネントごとのファイル分割(採用)

画面単位のテストファイルを、コンポーネント単位に分割する。

- メリット
	- 効率的なレンダリングが可能になる
		- 分岐網羅する際に子コンポーネント単体で行うため、それ以外の無関係な箇所のレンダリングが不要となる
	- コンポーネントとテストファイルが1：1の関係となるため、テスト追加する際に修正すべきファイルが明確になる
		- 保守性の向上
	- 1ファイル内のテスト数が減るため、見通しが良くなる
		- 可読性の向上
- デメリット
	- Layoutテストにおいては、親コンポーネントから子コンポーネントを含めた全体のレンダリングができなくなる
		- 以下の理由より、デメリットにはならないと判断
			- 画像ではなくHTMLのスナップショットのため、元々全体レンダリングによるレイアウト崩れは検知できない
			- 親コンポーネントのテストを残せば、全体レンダリングはできる

## 決定内容

- ヘルパー関数化
- コンポーネントごとのファイル分割

## 理由

- やるデメリットがなかったため
	- トレードオフがなかった
	- やらない理由が、まとまった時間が必要であること以外になかった

---

## 結果

- ヘルパー関数を適用したことで
	- テストケースごとの差分が一目で分かるようになった
		- それにより、不要なテストケース(全く同じことをしているなど)に気づくことができ、テスト数を削減できた
	- テストの追加がしやすくなった
		- 差分が見やすくなった
	- 保守性が向上した
		- コンポーネントのPropsを追加すると、そのコンポーネントをレンダリングしている全テストケースの修正が必要となり無駄な工数がかかっていたが、ヘルパー関数の1箇所を修正するだけで済むようになった
- コンポーネントごとに分割したことで
	- 非効率的なテストに気づけるようになった
		- 子コンポーネントのみをテストすればいいのに、親コンポーネントからレンダリングしているなど
	- テストファイルが小さくなった
		- 見通しが良くなり、保守性・可読性が向上した
		- 性能向上した
			- vitestでは並列実行する場合、テストファイルごとにスレッドが割り当てられるため、テスト数が多いファイルがあるとボトルネックになってしまう
			- ファイルを分割したことで、明確なボトルネックとなるファイルがなくなり、並列実行を活かせるようになった
- 具体的な成果
	- コード削減
		- 28210行→9956行
			- 約65%削減
	- テスト数
		- 446件→395件
			- 重複削除
	- 処理時間
		- 124.98s→69.79s

## 学び

- リファクタリング方針について話し合うだけだと、方向性はある程度絞れてもそこからアクションに繋げにくかったが、サンプルとして実際に1機能修正してみせると、どう変わるかが可視化されて話が進めやすかった
