---
created: 2026-07-31
updated: 2026-07-31
implemented: 2026-06-01
tags: ADR
---

# ADR-011: バッチ手動実行ワークフローの構築

## 背景・課題

* バッチ実行手順がドキュメント化されておらず、属人化している
* 現在のバッチ手動実行にはいくつか方法があるが、それぞれ課題がある

- EventBridgeのスケジュールを手動で変更する
   - 時間を1分後などに設定してバッチ実行後、設定を戻す方法
   - 問題点
      - 無効だったら有効にしないといけない
      - 戻すために設定を覚えておく必要がある
         - バッチをデプロイし直すことで再適用することもできるが、いずれにせよ手間がある
- SSM Run Commandで直接バッチ実行
   - 問題点
      - ログをRun Commandで見れる形式で出力するようコマンドが整形されているが、それを毎回メモからコピペしないといけない
- EC2サーバーに乗り込んで直接バッチ実行
   - 問題点
      - バッチ実行のためにルートユーザーになる必要がある
         - 危ういし、ドキュメント化しづらい

上記3つに共通する問題点として、AWSコンソールに入る手間がある。

そこで、任意のバッチを任意の環境で安全かつ簡単に手動実行できるようにしたかった。

## やったこと

GitHub Actionsのワークフローで、EventBridge → SSM Run Commandという既存アーキテクチャを再利用しつつバッチ実行できる仕組みを構築した。

## 検討した選択肢

### 実行制限の要否

#### 選択肢A：制限を設けない（誰でも実行可能）

- 採用：No
- メリット：運用がシンプル
- デメリット：
  - バッチの仕様を理解していない人でも実行できてしまう
	  - 誰でも実行できると、開発用データが壊れる危険性がある

#### 選択肢B：実行者を制限する

- 採用：Yes
- 採用理由：
  - 誤操作による開発用データ破壊を防げる
  - バッチの仕様を理解している人のみ実行できるようにできる
- メリット：安全性が高い
- デメリット：制限管理の手間が発生する（後述の管理方法で対応）

### 実行環境の制限（本番実行の可否）

#### 選択肢A：承認付きで本番実行も許可する

- 採用：No
- メリット：本番での緊急対応がしやすくなる
- デメリット：
  - リスクがある
  - 本番でバッチを手動実行したいタイミングが現状なさそう
  - 仮に必要になったとしても、既存のスケジュール一時変更で対応可能

#### 選択肢B：実行環境から本番を除外する

- 採用：Yes
- 採用理由：
  - リスク回避
  - 本番での手動実行ニーズが想定できない
  - 必要であれば既存の手動対応（スケジュール変更）で代替できる
- メリット：安全性が高い
- デメリット：本番での手動実行手段が既存のスケジュール変更のみに限られる

### 認証方法

#### 選択肢A：GitHubアカウントで制限

- 採用：Yes
- 採用理由：
  - GitHub APIの呼び出しが不要
  - メールアドレスが変更された際に漏れるリスクがない
	  - 実運用上メアド変更自体は想定しづらいが
- メリット：実装・運用がシンプル
- デメリット：特になし

#### 選択肢B：メールアドレスで制限

- 採用：No
- メリット：組織のメール体系に基づいた制限ができる
- デメリット：
  - GitHub APIの呼び出しが必要
  - メアド変更時に修正漏れのリスクがある

### 制限リストの管理方法

#### 選択肢A：テキストファイルで管理

- 採用：Yes
- 採用理由：
	- 利便性が高いため
	- 編集自体は誰でも可能だが、必ずPRレビューを通すため、不正なユーザーを勝手に設定することはできず、運用上問題なしと判断した
- メリット：動作確認がしやすい
- デメリット：誰でも編集可能

#### 選択肢B：GitHubのEnvironmentsで管理

- 採用：No
- メリット：GitHub標準機能で管理できる
- デメリット：
  - 設定にプロパー社員への依頼が必要
  - 動作確認がしづらい

---

## 結果

- コンソールにログインせずにバッチが実行できるようになった
- 誰がどのバッチを実行したかという履歴が残るようになった
- 開発メンバーに使ってもらい、楽になったという感想をもらった

## 学び

* KiroのSpecモードで開発してみたが、難しかった
  * 途中で仕様変更すると、Requirementsから修正が必要になる
  * ところどころ修正が漏れて、ドキュメント間で不整合が起きることがあった
* GitHub Actionsのワークフロー実行UIには制約があることを学んだ
  * fzfのように、入力欄にバッチ名を入れると自動的に曖昧検索でサジェストが出る、という作りにはできなかった
  * 苦肉の策として、以下の仕様で対応した
    * `_list`と入力して実行すると、バッチ一覧が結果に表示される
    * バッチ名の一部が合致する文言を入力して実行すると、その文言を含むバッチ名が結果に表示される（曖昧検索）