---
created: 2026-07-26
updated: 2026-07-26
implemented: 2025-09-01
tags: ADR
---

# ADR-009: 非同期処理のNewRelicトレーシング整備

## 対応期間

2025/09下旬

## 背景・課題

- 非同期処理のトレースが親トランザクションと紐づかず、実行状況の追跡ができていなかった
	- 非同期処理用にコンテキストをコピーしていたが、ハンドラー終了時点でNewRelicの親トランザクションが閉じられるため、非同期処理からのトレース情報を親トランザクションに紐づけることができなかった
- 非同期処理で発生するエラーログがNewRelicに送信されない事象が発生しており、エラーの見落としリスクがあった
	- 事象の再現性が低く原因特定のコストが高いため、特定より欠損時でも検知できる仕組みの整備を優先した

## 検討した選択肢

### トレースの紐付け方法

#### コンテキストコピーのみ（従来）

- 採用：No
- メリット：
	- 実装がシンプルで追加の構造変更が不要
- デメリット：
	- ハンドラー終了時点で親トランザクションが閉じられ、非同期処理のトレースを紐づけられない
- 判断：
	- 構造上の問題を解決できないため却下

#### 分散トレーシングヘッダー＋独立トランザクション

- 採用：Yes
- 修正内容：
	- 分散トレーシングヘッダー（`InsertDistributedTraceHeaders` / `AcceptDistributedTraceHeaders`）で親子関係を確立した上で、非同期処理用の独立トランザクションを開始する構造に一律で変更
	- 独立トランザクションは明示的に `End()` を呼ぶ必要があるため、goroutine内で生成しdeferでEndする方式に統一（`txn.NewGoroutine()` を外側から渡すと親トランザクション終了時に道連れで終了してしまうため）
	- トランザクション名・セグメント名の命名規則も統一（`Background/ProcessName/SubCategory`、`async.launcher.xxx` / `async.worker.xxx`）
- メリット：
	- 呼び出し元・呼び出し先双方の処理時間が可視化され、問題の切り分けが容易になる
	- NewRelic UI上で親子関係が線で結ばれ、エンドツーエンドの追跡が可能になる
- デメリット：
	- 全非同期処理箇所に対して構造変更・動作確認が必要
- 判断：
	- コンテキストコピーのみでは解決できない構造的な問題のため採用

### エラー検知方法

#### エラーログ出力のみ（従来）

- 採用：No
- メリット：
	- 実装がシンプルで追加対応が不要
- デメリット：
	- 非同期処理内でエラーが発生してもNewRelicに送信されない場合があり、見落としリスクがある
- 判断：
	- 事象の再現性が低く原因特定のコストが高いため、ログ出力のみでは見落としリスクを解消できず却下

#### NoticeErrorで明示的に送信

- 採用：Yes
- 修正内容：
	- 非同期処理内のエラーをチャネルで拾い、`NoticeError` で明示的にNewRelicへ送信する仕組みを整備
- メリット：
	- エラーログ欠損時にも確実に検知できる体制になる
- デメリット：
	- 全非同期処理箇所に対して実装追加が必要
- 判断：
	- 原因特定より欠損時でも検知できる仕組みを優先し採用

### 対応タイミング

#### 個別対応

- 採用：No
- メリット：
	- 1回あたりの変更範囲を小さくできる
- デメリット：
	- 非同期処理ごとに個別の修正・動作確認が発生し、対応の度に再修正・再テストのコストがかかる
- 判断：
	- トレース紐付けの修正でも全非同期処理箇所を触るため、別タイミングにすると修正・動作確認コストが二重に発生する

#### トレース整備と一括対応

- 採用：Yes
- メリット：
	- トレース紐付けの修正と同じタイミング（全非同期処理を修正する機会）に合わせることで、修正・動作確認の手間をまとめられる
- デメリット：
	- 一度の変更範囲が大きくなる
- 判断：
	- 個別対応すると再修正・再テストのコストが重複するため、修正コストを考慮し一括対応を採用

---

## 結果

- 非同期処理のトレースが親トランザクションと紐づき、エンドツーエンドで監視できる状態を実現
- エラーログが欠損した場合でも、エラーを確実に検知できる体制を整備

## 学び
- NewRelicのトランザクションの仕組みを学んだ
