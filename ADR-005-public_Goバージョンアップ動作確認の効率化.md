---
created: 2026-06-17
updated: 2026-06-17
implemented: 2026-03-01
tags: ADR
---
# ADR-005-public: Goバージョンアップ動作確認の効率化

## 対応時期

2026年3月

## 背景・課題

GoのバージョンアップのたびにメールやSMS、チャットツール、外部サービスコールバックなど
各種通知チャネルを含む機能の動作確認を手動で行っており、以下の課題があった。

- 確認対象のAPIやバッチが複数あり、実行に必要なデータの準備や手順の把握に時間がかかる
- ノウハウが特定の有識者に偏りがちで、属人化していた
- Goバージョンアップは頻繁に発生するイベントであり、そのたびに工数がかかっていた

また、バージョンアップ作業自体も以下のような手動対応が多く、ミスや確認コストが発生していた。

- ツールごとにGitHubで最新バージョンを手動確認
- 複数のDockerfileを手動更新
- コンテナリビルドとテスト確認を手動実施
- ファイルの修正漏れをレビューで確認

## 検討した選択肢

### バージョンアップ作業の自動化

#### 選択肢A: シェルスクリプト・Makefileコマンドで自動化する
- 採用：Yes
- 実装：
  - Dockerfileありのプロジェクト：シェルスクリプトで対応
    （最新バージョン取得・Dockerfile更新・go.sum更新・コンテナリビルド・go test実行）
  - Dockerfileなしのプロジェクト：Makefileにコマンドを追加
    （go.sum更新・go test実行）
- メリット：
  - バージョンアップ作業を1コマンドで完了できる
  - 手順の抜け漏れがなくなる
  - ファイルの修正漏れをレビューで確認する必要がなくなる
- デメリット：
  - シェルスクリプト自体のメンテナンスが必要
- 判断：作業コスト・レビューコストの削減効果が大きいため採用

#### 選択肢B: 手動で行う（現状維持）
- 採用：No
- デメリット：
  - 課題がそのまま残る

---

### スモークテスト用エンドポイント・バッチの新規作成

#### 選択肢A: 動確用エンドポイント・バッチを新規作成する
- 採用：Yes
- メリット：
  - アカウントIDとユーザーIDを引数として渡すだけで全外部連携の疎通確認が1発で完了できる
  - 意図しない宛先への通知や開発データの更新が起こらない
  - 誰でも・漏れなく実行できるようになり、属人化が解消される
  - 既存コードに動確用の分岐を入れないため、本番コードへの影響がない
- デメリット：
  - 動確エンドポイント自体のメンテナンスが必要（新規外部連携追加時の更新漏れリスク）
  - これまでは手動で各機能を触ることでプロダクトの理解が深まる副次的な効果があったが、それが失われる
	  - ただしGoバージョンアップの副次的な効果に過ぎず、必要になれば別途勉強できるため大きな問題ではないと判断した
- 実装上の懸念と対応：
  - 本番環境での誤実行防止：環境変数チェックをエントリポイントに追加し、dev環境以外では実行できないようにした
  - 既存コードパスとの乖離：`SmokeTestService` から既存の外部連携処理と既存Repositoryを直接呼び出す構成にすることで、本番と同じコードを通るようにした
- 判断：課題に対して素直に刺さる解決策であり、デメリットも許容範囲

#### 選択肢B: 既存のAPIを個別に手動で叩く（現状維持）
- 採用：No
- メリット：
  - 新規実装が不要
- デメリット：
  - 課題がそのまま残る

---

### 通知先の制御方法

#### 選択肢A: リクエスト引数で渡す
- 採用：Yes
- メリット：
  - 実行のたびに送信先を柔軟に指定できる
  - 既存コードへの変更が不要
- デメリット：
  - 特になし

#### 選択肢B: 環境変数で差し替える
- 採用：No
- メリット：
  - 引数を意識せず実行できる
- デメリット：
  - 通知先はDBから取得しているため、環境変数で差し替えるための分岐を既存コードに追加する必要があり、コードが汚れる
- 判断：既存コードへの影響が生じるため不採用

---

### 外部連携の接続先

#### 選択肢A: 本物の外部サービスに接続する
- 採用：Yes
- メリット：
  - Goバージョンアップ後に外部サービスとの通信が正常に動くことを確認できる
- デメリット：
  - 実行環境や送信先の管理が必要

#### 選択肢B: モックに接続する
- 採用：No
- メリット：
  - 実行環境を選ばない
- デメリット：
  - テストコードと目的が同じになり、実際の疎通確認にならない
- 判断：疎通確認が目的であるため不採用

---

## 結果

以下の対応を実施した。

**バージョンアップ作業のシェル化**
- Go本体・ツール類の最新バージョン取得、Dockerfile・README等の更新、
  コンテナリビルド、go testの実行までをシェル1発で完了できるようにした
- 手動でのバージョン確認・ファイル修正・修正漏れのレビューが不要になった

**スモークテストの整備**
- `SmokeTestController` / `SmokeTestService`：外部連携処理を直接呼び出す動確用エンドポイント
- スモークテストバッチ：送信先を引数として受け取り、エンドポイントを叩く

バージョンアップ時の作業が以下のみとなり、これまで1日かかっていた作業が1時間で完了するようになった。

1. シェルまたはmakeコマンドを実行（バージョン更新・テストまで自動化）
2. スモークテストバッチを実行（全外部連携の疎通確認）
3. 社内管理ツールからローカルでリクエスト送信して確認

---

## 関連
- [[ADR-006-public_スモークテスト実装上の設計判断]]
