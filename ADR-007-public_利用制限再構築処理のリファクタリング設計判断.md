---
created: 2026-06-17
updated: 2026-06-17
implemented: 2026-04-01
tags: ADR
---

# ADR-007-public: 利用制限再構築処理のリファクタリング設計判断

## 背景・課題

利用制限の再構築処理において、シミュレートとDB登録が分離されておらず、以下の問題があった。

- ロジックの中にDB登録とエラーハンドリングが混在し、可読性が低い
- 親レコードのIDを取得するために都度DB登録が必要な構造になっており、処理の流れが複雑だった
- シミュレート用の構造体が複数あり、何が入っているか・どのように使われるかをソースを読むだけで把握するのが困難だった
- 場当たり的な対処が積み重なっており、コードの正当性（なぜそうするか）が判断できない箇所が多かった
- 新規SQLが多く、差分や使い分けの把握が困難だった
- 総じて実装者しか触れない状態になるリスクがあった

## 検討した選択肢

### シミュレートとDB登録の分離

#### 選択肢A: シミュレート処理を分離し、その結果をDB登録処理に渡す

- 採用：Yes
- 採用理由：シミュレート部分の単体テストが書きやすくなり、責務の分離による可読性向上の効果が大きい。副次的にSQLの本数も削減できる
- メリット：
    - シミュレート部分の単体テストが書きやすくなる
    - 責務が明確になり、可読性が上がる
    - DB登録の副作用なしにロジックを検証できる
    - 副次的にSQLの本数も削減できた
- デメリット：
    - 分離のための構造変更コストが発生する

#### 選択肢B: 現状の構造を維持する

- 採用：No
- デメリット：
    - 課題がそのまま残り、実装者以外が触れない状態が固定化される

---

### デフォルト値（終了日：無期限）の設定箇所

#### 選択肢A: シミュレート部分で終了日を設定し、リポジトリ層には常に値がある状態で渡す

- 採用：Yes
- 採用理由：「終了日未設定＝無期限」というビジネスルールがドメイン層に収まり、リポジトリ層の責務が明確になる。ビジネスロジックが永続化層に漏れている状態を解消できる
- メリット：
    - シミュレート部分でどんな値が入るかが明確になる
    - リポジトリ層は渡された値をDB登録するだけになり、責務が明確
- デメリット：
    - 特になし

#### 選択肢B: リポジトリ層でnilチェックしてデフォルト値を設定する（現状）

- 採用：No
- デメリット：
    - ビジネスロジックが永続化層に漏れており、責務が不明確
    - リポジトリ層を読んだときに「なぜここでnilチェックしているのか」を理解するコストが生まれる

---

### 構造体の整理

#### 選択肢A: 役割を明確にした構造体に整理する

- 採用：Yes
- 採用理由：何が入っているかが明確になり、更新タイミングがたどりやすくなる。マッピング・インデックス用の変数を削減でき、処理の流れを追うコストが大幅に下がる
- 構成：
    - シミュレートへの入力を入れる構造体
    - シミュレート結果を入れる構造体（そのままDB登録値になる）
    - テーブルごとの構造体
- メリット：
    - 何が入っているかが明確になる
    - 更新タイミングがたどりやすくなる
    - マッピング・インデックス用の変数を削減でき、可読性が上がる
- デメリット：
    - 構造変更のコストが発生する

#### 選択肢B: 既存の構造体を維持する（現状）

- 採用：No
- デメリット：
    - 処理の流れを正確に追えないと何が入っているかわからない状態が継続する

---

### テストデータの方針

#### 選択肢A: テストデータは実際に起こりうるシナリオを反映する

- 採用：Yes
- 採用理由：シミュレーション内部で開始日順の処理・日時分岐があるため、非現実的なデータでは検出できないバグが生じうる。またテストコードは仕様の実例として読まれるため、非現実的なデータは仕様誤認のリスクになる
- メリット：
    - 実際のコードパスを通るテストになる
    - テストコードを読んで仕様を学習する際に誤認が生じない
- デメリット：
    - テストデータの作成コストが上がる場合がある

#### 選択肢B: 分岐網羅を優先し、テストデータの現実性は問わない

- 採用：No
- デメリット：
    - 本番では発生しないシナリオでテストが通っても、実際の動作の正しさを証明できない
    - テストコードから仕様を誤認するリスクがある

---

## 結果

- シミュレートとDB登録の分離により、テスタビリティと可読性が大幅に向上した
- ビジネスロジックがドメイン層に集約され、リポジトリ層の責務が明確になった
- 構造体の整理とマッピング変数の削減により、コードの追跡コストが下がった
- 実装者以外でも触れる状態を維持できる見通しが立った

## 学び

- 可読性の問題は局所的な指摘では解消できないことがある。構造全体に問題が及んでいる場合は、修正例を示す形のレビューが有効
- テストコードは仕様の実例として読まれる。非現実的なデータは仕様誤認のリスクになる
- 改善の価値は「起きなかった問題」として見えにくいため、「放置した場合のリスク」で語ると伝わりやすい
